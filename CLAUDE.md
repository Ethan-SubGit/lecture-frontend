# 프로젝트 규칙

## 서브에이전트 역할

새 기능/화면 작업은 아래 순서로 서브에이전트를 사용한다. 각 에이전트는 이전 대화를 보지 못하고 앞 단계가 남긴 파일만 읽으므로, 산출물은 항상 파일로 남긴다.

1. **spec-architect** — 사용자의 기능 요청을 구현 가능한 스펙(`REQ/<slug>/spec.md`)으로 변환한다. 모호한 요구사항 정리, 수용 기준 정의, 화면·데이터 모델 도출. 새 기능/화면/페이지 요청을 받으면 코딩 시작 전 가장 먼저 사용한다.
2. **ui-ux-designer** — spec.md를 바탕으로 화면 레이아웃, 반응형 동작, 디자인 토큰, 컴포넌트 시각 명세(`REQ/<slug>/design.md`)를 작성한다. 기존 화면의 반응형 개선이나 디자인 시스템 정리에도 사용한다.
3. **frontend-dev** — spec.md와 design.md가 준비된 뒤, Next.js + TypeScript + Tailwind로 실제 코드를 구현한다. 컴포넌트 작성, 리팩터링, 기능 구현을 담당한다.

세 에이전트 모두 [code-rules](.claude/skills/code-rules.md)의 한글 주석 규칙을 따른다.

## 코드 관리 (git)

**git-ops** — 변경사항 커밋과 GitHub(`origin` = `https://github.com/Ethan-SubGit/lecture-frontend`) 푸시를 전담한다. 사용자가 "커밋해줘", "코드 업데이트해줘", "깃허브에 올려줘"라고 요청하면 사용한다. 실제 코드를 작성/수정하지 않으며, 스펙·디자인·구현 파이프라인(spec-architect/ui-ux-designer/frontend-dev)과는 독립적으로 호출된다.

## 한글 주석 필수

디자인과 코드를 생성/수정할 때는 반드시 상세한 한글 주석을 단다. 자세한 규칙은 [.claude/skills/code-rules.md](.claude/skills/code-rules.md) 참고.

- 새로 작성하거나 수정하는 모든 코드에 한글로 상세한 주석을 단다.
- 함수/컴포넌트 상단에는 역할, 매개변수, 반환값을 한글로 설명한다.
- 복잡한 로직·조건 분기·예외 처리에는 왜 그렇게 처리하는지 한글 주석을 추가한다.
- 디자인 명세(design.md 등)에도 컴포넌트/레이아웃의 의도를 한글로 설명한다.
- 영어 주석만 있거나 주석이 없는 코드는 미완성으로 간주하고, 손이 닿은 부분은 한글 주석을 보강한다.
